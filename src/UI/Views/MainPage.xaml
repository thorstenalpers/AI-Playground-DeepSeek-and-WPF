<Page
    x:Class="DeepSeek.WPF.UI.Views.MainPage"
    Style="{DynamicResource MahApps.Styles.Page}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:properties="clr-namespace:DeepSeek.WPF.UI.Properties"
	xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
	xmlns:vm="clr-namespace:DeepSeek.WPF.UI.ViewModels"
    mc:Ignorable="d"
    d:DesignHeight="450" d:DesignWidth="800">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="48" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<TextBlock
            Style="{StaticResource PageTitleStyle}"
            Margin="{StaticResource MediumLeftMargin}"
            Text="{x:Static properties:Resources.MainPageTitle}" />
		<Grid Grid.Row="1"
      Margin="{StaticResource MediumLeftRightMargin}"
      Background="{DynamicResource MahApps.Brushes.Gray10}">
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="*" />
			</Grid.RowDefinitions>

			<TextBlock Text="Ask something:"
               Margin="0,0,0,5"
               FontWeight="Bold"
               Grid.Row="0" />

			<!-- Dropdown for LLM model selection -->
			<TextBlock Text="Select LLM Model:"
               Margin="0,0,0,5"
               FontWeight="Bold"
               Grid.Row="1" />

			<ComboBox x:Name="ModelSelectorComboBox"
                      Width="200"
                      Margin="0,0,0,10"
                      SelectedItem="{Binding SelectedModel}"
                      ItemsSource="{Binding AvailableModels}">
			</ComboBox>

			<DockPanel LastChildFill="True"
               Margin="0,0,0,10"
               Grid.Row="1">
				<TextBox x:Name="UserInputTextBox"
                    Height="30"
                    Margin="0,0,10,0"
                    VerticalAlignment="Center"
                    mah:TextBoxHelper.Watermark="Type your question..." 
				    Text="{Binding UserInput, UpdateSourceTrigger=PropertyChanged}" />
				<Button x:Name="SendButton"
                    Content="Send"
                    Width="60"
                    Height="30" 
					Command="{Binding SendCommand}"/>
			</DockPanel>

			<!-- Output -->
			<TextBox x:Name="OutputTextBox"
                Grid.Row="2"
                VerticalScrollBarVisibility="Auto"
                AcceptsReturn="True"
                IsReadOnly="True"
                TextWrapping="Wrap"
                FontFamily="Consolas"
                Background="#f5f5f5"
                Padding="10"
			    Text="{Binding Output}" />
		</Grid>
	</Grid>
</Page>
